# on laisse tout par d?faut et donc on a une BD H2 en m?moire pour les tests tagg?s @DataJpaTest
# pour avoir dans la console Spring les details SQL ;)
spring.jpa.show-sql=true

# parametres pour JWT (pour les tests ce n'est pas un vrai secret)
application.security.jwt.secret-key=000000
# 86400000 # a day
application.security.jwt.expiration=60000
# 7 days
application.security.jwt.refresh-token.expiration=604800000